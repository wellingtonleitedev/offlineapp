import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  modal: {
    flex: 1,
    padding: 20,
  },
  container: {
    backgroundColor: '#ebebeb',
    borderColor: '#ccc',
    borderRadius: 8,
    borderWidth: 1,
    marginBottom: 10,
    padding: 16,
  },
});
